---
name: project_svd_route_2026_06_15
description: "Deterministic SVD renderer added after the 6-hourly probe flagged 'singular value decomposition of a 2x2 matrix' (route=None, no orthonormal U/V)"
metadata:
  node_type: memory
  type: project
  originSessionId: 99d729af-e760-4960-a041-8a1eccc50fb2
---

2026-06-15: the 6-hourly quality probe flagged "show the singular value decomposition of a 2x2 matrix" — route=None (LLM-SVG), retries exhausted, text outside viewBox + overlap + vision review FAIL ("lacks orthonormal columns for U and V"). Same class as the spectral theorem: a matrix decomposition the LLM draws unreliably. Fix = renderer-first (commit `c4cf1e1`, deployed).

`studio/templates/svd.py` (`is_svd_prompt`, `render_svd`, `generate_svd_svg`): canonical A=[[2,2],[−1,1]] with U=[[1,0],[0,−1]] (a reflection), Σ=diag(2√2, √2), V=(1/√2)[[1,1],[1,−1]] (the 45° rotation, symmetric so Vᵀ=V). Computes U Σ Vᵀ and ASSERTS it reproduces A AND that UᵀU=VᵀV=I before drawing. Reuses spectral.py's `_matrix` bracketed-grid helper. Includes the canonical **unit-circle→ellipse geometric panel** (right side): unit circle maps to an ellipse with semi-axes = singular values; v₁,v₂ drawn as orthonormal input vectors at ±45° (dashed blue), their images σ₁u₁ (along +x) and σ₂u₂ (along −y) solid red along the ellipse axes — this directly answers the "show orthonormal columns" vision complaint. Narration (5 phrases) highlights matrix ids (mU/mS/mVT/statement) + geometry ids (vvecs/uvecs/ellipse). Wired into express cascade right AFTER the spectral route, flag `SEVIM_SVD_ROUTE` (default on). `tests/test_svd.py`. 541 tests pass.

KEY (reused lesson): deterministic routes return `review_history=[], retries_used=0` and SKIP vision review entirely, so a vision-review FAIL like "lacks orthonormal columns" CANNOT recur once the class is deterministic. The probe reliably catches matrix-decomposition prompts (spectral, SVD) the LLM-SVG path can't structure. Renderer-first total now 9 (np_completeness, reduction, bayes_tree, normal_distribution, confusion_matrix, infinitude_primes, ftc, spectral, **svd**) + newton_intro + taylor_sin + clique_vertex_cover + sphere_surface_area + **sat_vertex_cover** (2026-06-19, 3SAT≤ₚVertexCover drawn as the ACTUAL gadget graph — variable-gadget edges + clause triangles + connecting edges + asserted size-k=n+2m vertex cover; this is the paper's Fig 1 case, was generic reduction schematic; flag SEVIM_SAT_VC_ROUTE, commit 1be46df) + **eigen_geometry** (2026-06-26, "explain eigenvalues and eigenvectors geometrically" — probe flagged the LLM NARRATION as mathematically WRONG ["eigenvectors do not rotate" — false for negative λ which flips them]; figure was visually fine. Deterministic A=[[2,1],[1,2]] plane: v1→3v1 stretched, v2 unchanged, generic u rotates off its line; asserts A·v=λv; narration says "reversed if the eigenvalue is negative". Excludes "spectral"+bare-compute. Flag SEVIM_EIGEN_GEOM_ROUTE, commit 0893c9d. KEY: deterministic routes skip review so a NARRATION-content error can't recur, same as a layout error) + **conditional_probability** (2026-06-17, Venn, probe-flagged "explain conditional probability with a Venn diagram" — structural rubric wanted explicit "is defined as"; commit de63901, flag SEVIM_CONDPROB_ROUTE) + 10 fractals. DOCS: README renderer-first paragraph + paper khayyam-math-paper renderer-first lists (4 spots) to be updated to include svd (was "nine further classes"→"ten"). See [[project_reduction_overlap_fix_2026_06_12]] (spectral + the renderer-first pattern), [[project_feedback_digest_2026_06_14]] (newton_intro/taylor_sin), [[feedback_deterministic_routes_no_fallback]].
